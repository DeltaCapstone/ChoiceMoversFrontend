import { CreateJobEstimate } from './create-job-estimate.model';

describe('CreateJobEstimate', () => {
  it('should create an instance', () => {
    expect(new CreateJobEstimate()).toBeTruthy();
  });
});
